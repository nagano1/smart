//#include "thread_pool.hpp"
#include <cstdio>
//#include <stdio.h>

bool stopped = false;
int wakeup_count = 0;

int main()
{
    printf("B");
    fflush(stdout);

    //exit(1);
    //assert(false);

    return 0;
}
